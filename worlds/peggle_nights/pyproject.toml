[project]
name = "worlds.peggle_nights"
version = "1.0.2"
description = "MultiWorld: Peggle Nights"
classifiers = ["Private :: Do Not Upload"]
requires-python = ">=3.12"
[[project.authors]]
name = "SerpentAI"

[project.entry-points."mwgg.client"]
"worlds.peggle_nights.Client" = "worlds.peggle_nights:launch_client"

[tool.setuptools.packages.find]
where = ["src"]
include = ["worlds.peggle_nights"]
namespaces = true
